template: template.base-live
id: live_behavior.otp_sent_explains_image_limitation
suite: live_behavior_regression
version: 1
description: After sending an email code, the assistant asks the user to copy and paste it without the confusing word text.
tags: [live, behavior, authentication, otp, image-limitation]
targetModes: [live_lambda]
inputs:
  - text: Mi correo registrado es regression-image-code@example.invalid.
    channel: terminal_whatsapp
    sessionId: otp-sent-image-guidance-session
seedPlan:
  current_node: resolver_consultas_informativas
  information_state:
    resume_node: entrevista
    pending_requests:
      - requestId: information-1
        kind: associated_event
        query: ¿A qué hora empieza el evento?
        eventHint: null
    selection_candidates: []
expectations:
  - id: requests-email-code-without-phone-lookup
    type: tool_usage
    mustCall: [request_user_login_code]
    mustNotCall: [auth_by_phone, verify_user_login_code]
    severity: hard
  - id: code-challenge-persists
    type: plan_field_equals
    path: user_auth.status
    expected: code_requested
    severity: hard
  - id: post-send-image-guidance
    type: text_semantic
    rubric: >-
      Pass only if the assistant responds naturally and entirely in Spanish that a one-time code was sent to the provided email, explicitly explains that it cannot read images or screenshots, and directly asks the user to copy and paste the code here. It must not use the word “texto” as an instruction, ask for the WhatsApp number, or claim that the protected event question has already been answered.
    minScore: 0.9
    requireJudge: true
    severity: hard
budget:
  maxTurns: 1
  estimatedPromptTokensPerTurn: 1800
  estimatedCompletionTokensPerTurn: 350
