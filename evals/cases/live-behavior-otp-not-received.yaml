template: template.base-live
id: live_behavior.otp_not_received_requires_response
suite: live_behavior_regression
version: 1
description: A repeated missing-code report after one resend must trigger a real handoff and preserve the protected question without another OTP loop.
tags: [live, behavior, authentication, otp, reported-interaction]
targetModes: [live_lambda]
inputs:
  - text: No me ha llegado
    channel: whatsapp
    contactPhone: "$PHONE_FIRST_FALLBACK_CONTACT_PHONE"
    sessionId: otp-not-received-session
seedPlan:
  current_node: resolver_consultas_informativas
  contact_email: regression-not-received@example.invalid
  user_auth:
    status: code_requested
    email: regression-not-received@example.invalid
    token: null
    token_expires_at: null
    last_error: null
    requested_at: "2026-08-08T21:57:40.134Z"
    failed_code_attempts: 0
    otp_send_attempts: 2
    otp_non_delivery_reports: 1
    awaiting_phone_confirmation: false
    auth_method: null
  information_state:
    resume_node: entrevista
    pending_requests:
      - requestId: information-1
        kind: associated_event
        query: ¿La restricción de vestir de blanco aplica a mujeres y varones?
        eventHint: Karem y Alfredo
    selection_candidates: []
expectations:
  - id: enters-human-handoff
    type: node_transition
    to: solicitar_agente_humano
    severity: hard
  - id: challenge-and-question-remain-pending
    type: plan_field_subset
    path: information_state.pending_requests
    expected:
      - kind: associated_event
        query: ¿La restricción de vestir de blanco aplica a mujeres y varones?
    severity: hard
  - id: missing-code-report-hands-off-without-another-code-operation
    type: tool_usage
    mustCall: [request_human_takeover]
    mustNotCall: [request_user_login_code, verify_user_login_code]
    severity: hard
  - id: handoff-is-persisted
    type: plan_field_equals
    path: human_escalation.status
    expected: requested
    severity: hard
  - id: ends-code-delivery-loop
    type: text_semantic
    rubric: >-
      The user reports for a second time that the one-time code has not arrived after the system already resent it once. Pass only if the assistant confirms in natural Spanish that human support has now been requested and leaves the conversation with that team. It must not ask for another email or code, repeat inbox instructions, claim authentication succeeded, or invent an answer to the protected event question.
    minScore: 0.9
    requireJudge: true
    severity: hard
budget:
  maxTurns: 1
  estimatedPromptTokensPerTurn: 1800
  estimatedCompletionTokensPerTurn: 350
